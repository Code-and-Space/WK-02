function setup() {
  createCanvas(600, 600);
}

function draw() {
  background(0);
//  Can we locate center of box at mouseX and mouseY
//  recMode(CENTER);
  rect(300, 300, 50, 50)

//  Other helpful built-in variables include width and height
  // rect(width, height, 50, 50)
}
